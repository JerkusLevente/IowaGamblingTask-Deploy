import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'IOWA-Gambling-Task',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
